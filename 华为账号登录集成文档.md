# 华为账号登录集成文档

## 概述

MoMo应用现已集成华为Account Kit，支持用户使用华为账号登录，提供更安全、便捷的身份认证体验。

## 功能特性

- ✅ 华为账号授权登录
- ✅ 静默登录（已登录用户自动登录）
- ✅ 获取用户基本信息（昵称、头像、OpenID）
- ✅ 登出功能
- ✅ 本地用户信息缓存
- ✅ 降级支持（设备ID匿名登录）

## 架构设计

### 1. 认证提供商模式

采用策略模式设计，支持多种认证方式：

```
AuthService (统一入口)
    ├── AuthProvider (抽象接口)
    │   ├── HuaweiAccountProvider (华为账号登录)
    │   └── DeviceAuthProvider (设备ID匿名登录)
    └── TokenManager (Token管理)
```

### 2. 核心组件

#### HuaweiAccountProvider

位置：`entry/src/main/ets/service/auth/HuaweiAccountProvider.ets`

主要功能：
- 静默登录：尝试使用已有授权自动登录
- 授权登录：拉起华为账号登录界面
- 用户信息获取：OpenID、UnionID、昵称、头像
- 本地缓存：使用Preferences存储用户信息

#### LoginPage

位置：`entry/src/main/ets/pages/LoginPage.ets`

主要功能：
- 华为账号登录按钮
- 游客登录按钮（设备ID匿名登录）
- 登录状态显示
- 登出功能

## 使用方法

### 1. 在EntryAbility中初始化认证服务

```typescript
import { AuthService } from '../service/auth';

export default class EntryAbility extends UIAbility {
  async onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): Promise<void> {
    // 初始化认证服务（使用华为账号）
    const authService = AuthService.get();
    authService.useHuaweiAccount(this.context);
    
    // 尝试自动登录
    try {
      await authService.initialize();
    } catch (error) {
      console.error('认证初始化失败:', error);
    }
  }
}
```

### 2. 在页面中使用登录功能

```typescript
import { AuthService } from '../service/auth';

@Entry
@Component
struct YourPage {
  private authService: AuthService = AuthService.get();
  
  async handleLogin(): Promise<void> {
    try {
      // 切换到华为账号提供商
      this.authService.useHuaweiAccount(getContext(this));
      
      // 执行登录
      const user = await this.authService.login();
      console.log('登录成功:', user.nickname);
    } catch (error) {
      console.error('登录失败:', error);
    }
  }
  
  async checkLoginStatus(): Promise<void> {
    const isLoggedIn = await this.authService.isLoggedIn();
    if (isLoggedIn) {
      const user = await this.authService.getCurrentUser();
      console.log('当前用户:', user?.nickname);
    }
  }
}
```

### 3. 获取访问Token

```typescript
// 获取有效的访问Token（自动刷新）
const token = await authService.getAccessToken();

// 在API请求中使用
fetch('https://api.example.com/data', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});
```

## 权限配置

已在 `module.json5` 中配置：

```json5
"requestPermissions": [
  {
    "name": "ohos.permission.INTERNET",
    "reason": "$string:internet_permission_reason"
  },
  {
    "name": "ohos.permission.GET_NETWORK_INFO",
    "reason": "$string:network_info_permission_reason"
  }
]
```

## API说明

### AuthService

```typescript
class AuthService {
  // 获取单例
  static get(): AuthService
  
  // 切换到华为账号登录
  useHuaweiAccount(context: UIAbilityContext): void
  
  // 初始化（自动登录）
  async initialize(): Promise<void>
  
  // 登录
  async login(): Promise<User>
  
  // 登出
  async logout(): Promise<void>
  
  // 获取当前用户
  async getCurrentUser(): Promise<User | null>
  
  // 检查登录状态
  async isLoggedIn(): Promise<boolean>
  
  // 获取访问Token
  async getAccessToken(): Promise<string>
  
  // 获取当前提供商名称
  getProviderName(): string
}
```

### User模型

```typescript
interface User {
  userId: string;        // 用户唯一标识（UnionID或OpenID）
  deviceId?: string;     // 设备ID（OpenID）
  nickname?: string;     // 用户昵称
  avatar?: string;       // 头像URL
  createdAt?: string;    // 创建时间
}
```

## 登录流程

### 华为账号登录流程

```
1. 用户点击"华为账号登录"
   ↓
2. HuaweiAccountProvider.login()
   ↓
3. 尝试静默登录
   ├─ 成功 → 返回用户信息
   └─ 失败 → 执行授权登录
   ↓
4. 授权登录（拉起华为账号界面）
   ├─ 用户同意 → 获取授权码和用户信息
   └─ 用户拒绝 → 抛出异常
   ↓
5. 保存用户信息到本地
   ↓
6. 返回LoginResponse（包含User和Token）
```

### 静默登录流程

```
1. 应用启动
   ↓
2. AuthService.initialize()
   ↓
3. HuaweiAccountProvider.trySilentLogin()
   ├─ 已授权 → 自动登录成功
   └─ 未授权 → 返回null，需要用户手动登录
```

## 开发和调试

### 测试登录页面

1. 修改 `EntryAbility.ets`，加载登录页面：

```typescript
onWindowStageCreate(windowStage: window.WindowStage): void {
  windowStage.loadContent('pages/LoginPage', (err) => {
    // ...
  });
}
```

2. 运行应用，测试登录功能

### 查看日志

所有认证相关日志都带有标签：
- `HuaweiAccountProvider`: 华为账号登录相关
- `AuthService`: 认证服务统一日志
- `LoginPage`: 登录页面日志

使用hilog查看：
```bash
hdc shell hilog | grep "HuaweiAccountProvider\|AuthService\|LoginPage"
```

## 常见问题

### Q1: 登录时提示"Context未设置"

**A:** 需要在调用登录前先设置Context：
```typescript
authService.useHuaweiAccount(getContext(this));
```

### Q2: 静默登录失败

**A:** 静默登录失败是正常的，系统会自动降级到授权登录。常见原因：
- 用户首次登录
- 用户已撤销授权
- 授权已过期

### Q3: 如何区分华为账号登录和设备ID登录？

**A:** 通过 `getProviderName()` 查看当前使用的认证方式：
```typescript
const providerName = authService.getProviderName();
// "华为账号登录" 或 "设备ID登录"
```

### Q4: 授权码如何换取后端Token？

**A:** 当前实现中，授权码存储在 `LoginResponse.token.accessToken` 中。实际应用中，需要：
1. 将授权码发送到后端
2. 后端调用华为服务器验证授权码
3. 后端生成自己的accessToken和refreshToken
4. 前端保存后端返回的Token

示例：
```typescript
// 在HuaweiAccountProvider.login()中
const authCode = authResult.authorizationCode;

// 发送到后端换取Token
const response = await fetch('https://your-backend.com/auth/huawei', {
  method: 'POST',
  body: JSON.stringify({ authorizationCode: authCode })
});

const { accessToken, refreshToken, expiresIn } = await response.json();
```

## 后续优化建议

1. **Token刷新**: 当前使用Mock Token，需要集成真实的后端Token刷新机制

2. **错误处理**: 完善各种错误场景的用户提示

3. **自动重试**: 网络失败时的自动重试机制

4. **安全加固**: 
   - Token加密存储
   - 防重放攻击
   - 安全传输

5. **用户体验**:
   - 添加加载动画
   - 优化登录流程
   - 记住登录状态

## 参考文档

- [HarmonyOS Account Kit开发指南](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/account-introduction-V5)
- [华为账号服务API参考](https://developer.huawei.com/consumer/cn/doc/harmonyos-references-V5/js-apis-authentication-V5)
- [HarmonyOS权限说明](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/permissions-for-all-V5)

## 更新日志

### 2024-09-18
- ✅ 初始版本：实现华为账号登录基础功能
- ✅ 支持静默登录和授权登录
- ✅ 创建LoginPage登录页面
- ✅ 完善文档和使用说明
