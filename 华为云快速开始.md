# 默默 Mo Mo - 快速开始（华为云版）

## 🎯 项目概述

**默默（Mo Mo）** 是一个基于HarmonyOS NEXT的AI桌宠应用，使用**华为云端云一体化服务**实现：
- ✅ 华为账号一键登录
- ✅ 云数据库存储用户数据
- ✅ 云函数调用AI服务（DeepSeek + 即梦AI）
- ✅ 云存储管理漫画图片

---

## 📋 前置条件

### 1. 开发环境
- **DevEco Studio**: 5.0.0+
- **HarmonyOS SDK**: API 12+
- **Node.js**: 18+ (用于云函数开发)

### 2. 华为开发者账号
- 已注册并实名认证
- 已创建AppGallery Connect项目
- 已下载 `agconnect-services.json`

### 3. API密钥
- **DeepSeek API Key**: 用于LLM服务
- **即梦AI密钥**: 用于文生图服务

---

## 🚀 快速开始

### Step 1: 克隆项目

```bash
git clone https://github.com/your-repo/momo.git
cd momo
```

### Step 2: 配置华为云服务

#### 2.1 放置配置文件

将下载的 `agconnect-services.json` 放到：
```
entry/src/main/resources/rawfile/agconnect-services.json
```

#### 2.2 配置AppGallery Connect

1. 登录 [AppGallery Connect](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html)
2. 按照 [华为云服务配置指南](华为云服务配置指南.md) 完成：
   - Account Kit 开通
   - Cloud DB 创建存储区和对象类型
   - Cloud Storage 创建存储桶
   - Cloud Functions 部署函数

### Step 3: 安装依赖

在DevEco Studio中：
```
File → Sync and Refresh Project
```

或使用命令行：
```bash
ohpm install
```

### Step 4: 配置云函数

#### 4.1 部署云函数

在AppGallery Connect后台：

1. **部署 llm-daily-summary:**
   - 上传 `cloud-functions/llm-daily-summary.js`
   - 配置环境变量：`DEEPSEEK_API_KEY`

2. **部署 llm-comic-script:**
   - 上传 `cloud-functions/llm-comic-script.js`
   - 配置环境变量：`DEEPSEEK_API_KEY`

3. **部署 image-generate:**
   - 上传 `cloud-functions/image-generate.js`
   - 配置环境变量：`JIMENG_ACCESS_KEY`, `JIMENG_SECRET_KEY`

#### 4.2 测试云函数

在云函数控制台点击「测试」，验证是否正常工作。

### Step 5: 运行应用

#### 5.1 连接真机

1. 打开HarmonyOS设备的开发者模式
2. USB连接到电脑
3. DevEco Studio识别设备

#### 5.2 运行

点击 DevEco Studio 的「Run」按钮，或：
```bash
hdc shell aa start -a EntryAbility -b com.agent.momo
```

---

## 📱 使用流程

### 首次启动

1. **账号登录**
   - 点击「华为账号登录」
   - 授权后自动创建用户信息
   - 配额初始化：LLM 20次/天，图片 5次/天

2. **添加今日事件**
   - 在「今日状态」页添加事件
   - 支持时间、内容、心情记录

3. **生成每日总结**
   - 点击「生成总结」
   - 云函数调用DeepSeek生成温馨总结
   - 自动保存到云数据库

4. **生成漫画分镜**
   - 点击「生成漫画」
   - 第一步：生成5格漫画脚本
   - 第二步：调用即梦AI生成图片（每张约30秒）
   - 图片自动上传到云存储

5. **查看历史记录**
   - 在「记忆花园」查看历史总结
   - 支持查看历史漫画
   - 数据自动云端同步

---

## 🔍 代码结构

```
entry/src/main/ets/
├── service/
│   ├── auth/                      # 认证模块
│   │   ├── AccountKitAuthProvider.ets  # 华为账号登录
│   │   ├── DeviceAuthProvider.ets      # 设备ID登录（备选）
│   │   ├── AuthService.ets             # 认证服务入口
│   │   └── TokenManager.ets            # Token管理
│   │
│   ├── cloud/                     # 华为云服务
│   │   ├── CloudServiceManager.ets     # 云服务管理器
│   │   ├── CloudDBService.ets          # 云数据库
│   │   ├── CloudStorageService.ets     # 云存储
│   │   ├── CloudFunctionService.ets    # 云函数
│   │   ├── CloudDBModels.ets           # 数据模型
│   │   └── index.ets                   # 统一导出
│   │
│   └── ai/                        # AI服务
│       ├── AIServiceCloud.ets          # AI服务（华为云版）
│       ├── AIService.ets               # AI服务（旧版，兼容）
│       ├── LLMService.ets              # LLM服务
│       └── ImageGenService.ets         # 文生图服务
│
├── pages/                         # 页面
│   ├── Index.ets                       # 主页
│   ├── A_TodayStatus.ets               # 今日状态
│   ├── C_MemoryGarden.ets              # 记忆花园
│   └── B_AITest.ets                    # AI测试页
│
└── entryability/
    └── EntryAbility.ets                # 应用入口
```

---

## 🧪 测试指南

### 测试1: 账号登录

```typescript
import { AuthService } from '../service/auth/AuthService';
import { AccountKitAuthProvider } from '../service/auth/AccountKitAuthProvider';

// 切换到华为账号登录
const authService = AuthService.get();
authService.switchProvider(new AccountKitAuthProvider());

// 登录
await authService.initialize();
const user = await authService.getCurrentUser();
console.log('当前用户:', user);
```

### 测试2: 云数据库

```typescript
import { CloudServiceManager } from '../service/cloud/CloudServiceManager';

const cloudManager = CloudServiceManager.get();
await cloudManager.initialize();

const cloudDB = cloudManager.getCloudDB();

// 获取用户信息
const user = await cloudDB.getUser('test_user_id');
console.log('用户信息:', user);

// 检查配额
const quota = await cloudDB.checkAndResetQuota('test_user_id');
console.log('配额信息:', quota);
```

### 测试3: 生成每日总结

```typescript
import { AIServiceCloud } from '../service/ai/AIServiceCloud';

const aiService = AIServiceCloud.get();
await aiService.initialize();

// 生成总结
const events = [
  { time: '09:00', content: '起床，阳光明媚' },
  { time: '12:00', content: '午餐，吃了美味的面条' },
  { time: '15:00', content: '运动，跑步5公里' }
];

const summary = await aiService.generateDailySummary(events, '开心');
console.log('每日总结:', summary);
```

### 测试4: 生成漫画

```typescript
// 生成脚本
const panels = await aiService.generateComicScript(summary, events);
console.log('漫画脚本:', panels);

// 生成图片
const imageUrls = await aiService.generateComicImages(panels);
console.log('漫画图片:', imageUrls);
```

---

## 🐛 故障排查

### 问题1: 登录失败

**现象:** `Account Kit initialization failed`

**检查项:**
- [ ] `agconnect-services.json` 是否正确放置
- [ ] 包名是否为 `com.agent.momo`
- [ ] OAuth 2.0客户端ID是否配置
- [ ] 签名证书SHA-256是否正确

**解决方案:**
```bash
# 重新获取签名
keytool -list -v -keystore ~/.ohos/config/auto_debug_app1.p12 -storepass 123456

# 在AppGallery Connect更新签名
```

### 问题2: 云数据库连接失败

**现象:** `CloudDB initialization failed`

**检查项:**
- [ ] 存储区是否创建（MomoDataZone）
- [ ] 对象类型是否已定义
- [ ] 网络权限是否添加

**解决方案:**
在 `module.json5` 确认权限：
```json
{
  "requestPermissions": [
    { "name": "ohos.permission.INTERNET" },
    { "name": "ohos.permission.GET_NETWORK_INFO" }
  ]
}
```

### 问题3: 云函数调用超时

**现象:** `Function call timeout`

**检查项:**
- [ ] 云函数是否部署成功
- [ ] 环境变量是否配置
- [ ] API密钥是否有效
- [ ] 函数超时设置是否足够（建议60秒）

**解决方案:**
```javascript
// 增加超时时间
const request = {
  functionName: 'llm-daily-summary',
  parameters: params,
  timeout: 120000  // 增加到120秒
};
```

### 问题4: 配额不足

**现象:** `LLM配额不足，请明天再试`

**检查项:**
- [ ] 是否超过每日配额（默认20次）
- [ ] 配额重置时间是否正确

**解决方案:**
```typescript
// 手动重置配额（测试用）
const user = await cloudDB.getUser(userId);
user.llmQuotaUsed = 0;
user.imageQuotaUsed = 0;
user.lastQuotaReset = Date.now();
await cloudDB.upsertUser(user);
```

---

## 📊 性能优化

### 1. 图片缓存

```typescript
// 启用本地缓存
const localPaths = await aiService.downloadComicImages(imageUrls);
// 下次直接从缓存读取，无需重新下载
```

### 2. 批量操作

```typescript
// 批量生成图片（并行）
const tasks = panels.map(panel => 
  cloudFunction.generateImage({ userId, prompt: panel.prompt })
);
const results = await Promise.all(tasks);
```

### 3. 配额检查前置

```typescript
// 生成前检查配额
const quota = await aiService.getUserQuota();
if (quota.llmRemaining < 1) {
  showToast('LLM配额不足，请明天再试');
  return;
}
```

---

## 📚 相关文档

- [华为云服务配置指南](华为云服务配置指南.md)
- [客户端测试指南](客户端测试指南.md)
- [AI服务架构文档](AI服务架构文档.md)
- [完整技术方案](完整技术方案.md)

---

## 🤝 技术支持

**遇到问题？**
- 查看 [故障排查](#故障排查) 章节
- 阅读 [华为开发者文档](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V5/application-dev-guide-V5)
- 查看项目 [开发日志](开发日志.md)

---

**文档生成时间：** 2026-09-18  
**版本：** 1.0.0 (华为云版)
