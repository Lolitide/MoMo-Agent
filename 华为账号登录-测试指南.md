# 华为账号登录 - 测试指南

## ✅ 配置已完成

所有必要的配置都已经完成：

### 1. 代码文件
- ✅ `HuaweiAccountProvider.ets` - 华为账号认证实现
- ✅ `LoginPage.ets` - 登录页面UI
- ✅ `AuthTestPage.ets` - 测试页面

### 2. 配置文件
- ✅ `EntryAbility.ets` - 已修改为加载 LoginPage
- ✅ `main_pages.json` - 已添加 LoginPage 和 AuthTestPage
- ✅ `module.json5` - 已添加网络权限
- ✅ `string.json` - 已添加权限说明文本

### 3. 认证服务
- ✅ `AuthService.ets` - 已添加 `useHuaweiAccount()` 方法
- ✅ `index.ets` - 已导出 HuaweiAccountProvider

## 🚀 在 DevEco Studio 中测试

### 第一步：打开项目

1. 打开 DevEco Studio
2. 打开 MoMo 项目（如果已打开，跳过此步骤）

### 第二步：连接设备

确保已连接华为手机或模拟器：

1. **真机测试**（推荐）：
   - 通过 USB 连接华为手机
   - 手机开启开发者模式和 USB 调试
   - 确保手机已登录华为账号

2. **模拟器测试**：
   - 启动 HarmonyOS 模拟器
   - 确保模拟器已配置华为账号

### 第三步：运行应用

1. 点击 DevEco Studio 顶部工具栏的 **运行按钮** ▶️
2. 或按快捷键 `Shift + F10`（Windows/Linux）/ `Control + R`（Mac）
3. 等待编译完成并自动安装到设备

### 第四步：测试登录功能

应用启动后，你会看到登录页面：

#### 测试华为账号登录

1. **点击"华为账号登录"按钮**
   - 应该会拉起华为账号授权页面
   - 如果是首次登录，需要用户授权

2. **在授权页面操作**：
   - 查看应用请求的权限（OpenID、Email、Profile）
   - 点击"同意并授权"按钮

3. **授权成功后**：
   - 自动返回应用
   - 显示"登录成功！"
   - 显示用户昵称和ID
   - 1.5秒后自动跳转到主页

4. **验证结果**：
   - 登录状态应显示用户信息
   - 可以看到用户昵称

#### 测试游客登录（设备ID登录）

1. **点击"游客登录"按钮**
   - 无需任何授权
   - 直接使用设备ID创建匿名账号

2. **登录成功后**：
   - 显示"登录成功！"
   - 显示生成的用户ID
   - 1.5秒后跳转到主页

#### 测试登出功能

如果已登录：

1. **点击"登出"按钮**
   - 清除用户信息
   - 登录状态变为"已登出"
   - 用户信息消失

2. **验证结果**：
   - 登录按钮重新可用
   - 可以再次登录

### 第五步：测试静默登录（重要！）

1. **关闭应用**（完全退出）

2. **重新打开应用**

3. **观察登录状态**：
   - 如果之前使用华为账号登录过
   - 应该会自动静默登录
   - 无需再次授权
   - 直接显示用户信息

### 第六步：查看日志（可选）

如果需要查看详细日志：

1. **在 DevEco Studio 中**：
   - 打开底部的"Log"面板
   - 筛选标签：`HuaweiAccountProvider` 或 `AuthService` 或 `LoginPage`

2. **使用 hdc 命令**：
   ```bash
   hdc shell hilog | grep "HuaweiAccountProvider\|AuthService\|LoginPage"
   ```

## 🧪 使用测试页面（可选）

如果想使用功能更全面的测试页面：

### 修改 EntryAbility.ets

将第 48 行的 `LoginPage` 改为 `AuthTestPage`：

```typescript
windowStage.loadContent('pages/AuthTestPage', (err) => {
```

### 测试页面功能

测试页面提供了更多功能：

- ✅ 实时显示登录状态
- ✅ 显示当前认证提供商
- ✅ 显示用户完整信息
- ✅ 测试获取Token
- ✅ 刷新状态按钮
- ✅ 操作日志记录

## 📊 预期结果

### 华为账号登录成功

```
登录状态：✅ 已登录
认证方式：华为账号登录
用户昵称：[你的华为账号昵称]
用户ID：[UnionID或OpenID]
```

### 设备ID登录成功

```
登录状态：✅ 已登录
认证方式：设备ID登录
用户昵称：用户xxxxxx
用户ID：u_xxxxxxxx_timestamp
```

## ⚠️ 常见问题排查

### 问题1：编译错误 - 找不到 authentication

**症状**：编译时提示找不到 `@kit.AccountKit` 或 `authentication` 模块

**解决方案**：
1. 确认使用 HarmonyOS NEXT SDK（API 12+）
2. 检查 `oh-package.json5` 中的依赖配置
3. 同步项目：File → Sync Project

### 问题2：点击登录按钮没有反应

**症状**：点击"华为账号登录"按钮后没有弹出授权页面

**排查步骤**：
1. 查看日志，找错误信息：
   ```bash
   hdc shell hilog | grep "ERROR"
   ```

2. 常见原因：
   - Context 未设置（应该不会，代码中已处理）
   - 设备未登录华为账号
   - 网络连接问题

3. 解决方案：
   - 确保设备已登录华为账号
   - 检查网络连接
   - 重启应用重试

### 问题3：授权后闪退或无响应

**症状**：在华为账号授权页面点击"同意"后，应用闪退或卡住

**排查步骤**：
1. 查看崩溃日志：
   ```bash
   hdc shell hilog | grep "ERROR\|FATAL"
   ```

2. 常见原因：
   - 回调处理出错
   - 用户信息解析失败

3. 解决方案：
   - 查看详细日志定位问题
   - 检查 HuaweiAccountProvider 中的错误处理

### 问题4：静默登录总是失败

**症状**：重新打开应用时，没有自动登录

**说明**：这可能是正常的！

静默登录失败的常见原因：
- 首次登录（还没有授权记录）
- 授权已过期
- 用户主动撤销了授权

**解决方案**：
- 这是预期行为，用户需要手动登录
- 授权成功后，下次应该能静默登录

### 问题5：权限请求被拒绝

**症状**：应用启动时提示网络权限被拒绝

**解决方案**：
1. 在设备设置中找到应用
2. 进入权限管理
3. 手动开启"网络"权限
4. 重启应用

## ✅ 测试检查清单

完成以下所有测试项：

- [ ] 应用能正常启动并显示登录页面
- [ ] 点击"华为账号登录"能拉起授权页面
- [ ] 授权成功后能正常返回应用
- [ ] 能正确显示用户昵称和ID
- [ ] 点击"游客登录"能直接登录
- [ ] 登出功能正常，能清除用户信息
- [ ] 重新打开应用能自动静默登录（华为账号）
- [ ] 日志中没有错误信息
- [ ] UI响应流畅，没有卡顿

## 📱 测试设备要求

### 推荐配置
- HarmonyOS NEXT 设备
- 已登录华为账号
- 网络连接正常

### 最低要求
- HarmonyOS API 12+
- 支持 Account Kit

## 🎯 测试通过标准

所有以下条件都满足，即测试通过：

1. ✅ 华为账号登录流程完整
2. ✅ 能获取并显示用户信息
3. ✅ 静默登录工作正常
4. ✅ 登出功能正常
5. ✅ 无错误日志
6. ✅ UI显示正确

## 📝 测试报告模板

测试完成后，可以记录结果：

```
测试时间：2024-09-18
测试设备：[设备型号]
系统版本：HarmonyOS NEXT [版本号]

【华为账号登录】
- 拉起授权页面：✅ / ❌
- 授权成功：✅ / ❌
- 显示用户信息：✅ / ❌
- 静默登录：✅ / ❌

【游客登录】
- 登录成功：✅ / ❌
- 生成用户ID：✅ / ❌

【登出功能】
- 清除信息：✅ / ❌

【其他问题】
[记录遇到的问题]
```

## 🆘 需要帮助？

如果测试过程中遇到问题：

1. 查看日志找错误信息
2. 参考"常见问题排查"章节
3. 检查[完整技术文档](./华为账号登录集成文档.md)
4. 联系开发团队

---

**准备好了吗？现在就在 DevEco Studio 中点击运行按钮开始测试吧！** 🚀
