# 华为云服务配置指南

## 📋 目录

1. [环境准备](#环境准备)
2. [Account Kit配置](#account-kit配置)
3. [Cloud DB配置](#cloud-db配置)
4. [Cloud Storage配置](#cloud-storage配置)
5. [Cloud Functions配置](#cloud-functions配置)
6. [API密钥配置](#api密钥配置)
7. [客户端配置](#客户端配置)

---

## 🔧 环境准备

### 1. 注册华为开发者账号

1. 访问 [华为开发者联盟](https://developer.huawei.com/)
2. 注册并完成实名认证
3. 创建应用项目

### 2. 创建应用

1. 登录 [AppGallery Connect](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html)
2. 点击「我的项目」→「添加项目」
3. 项目名称：`MoMo`
4. 点击「添加应用」→「HarmonyOS」
5. 应用包名：`com.agent.momo`

### 3. 下载配置文件

1. 进入「项目设置」→「常规」
2. 下载 `agconnect-services.json`
3. 将文件放到：`entry/src/main/resources/rawfile/`

---

## 🔐 Account Kit配置

### 1. 开启Account Kit

1. 进入「构建」→「Account Kit」
2. 点击「立即开通」
3. 选择认证方式：
   - ✅ 华为账号登录
   - ✅ 匿名登录（可选）

### 2. 配置OAuth 2.0

1. 在「凭据」页面创建OAuth 2.0客户端ID
2. 应用类型：`Native Application`
3. 包名：`com.agent.momo`
4. 签名证书SHA-256：从DevEco Studio获取

**获取签名SHA-256：**
```bash
# 在DevEco Studio终端执行
keytool -list -v -keystore ~/.ohos/config/auto_debug_app1.p12 -storepass 123456
```

### 3. 配置应用权限

在 `module.json5` 添加：
```json
{
  "requestPermissions": [
    {
      "name": "ohos.permission.GET_NETWORK_INFO"
    },
    {
      "name": "ohos.permission.INTERNET"
    }
  ]
}
```

---

## 💾 Cloud DB配置

### 1. 创建存储区

1. 进入「构建」→「云数据库」
2. 点击「添加存储区」
3. 存储区名称：`MomoDataZone`
4. 数据加密：✅ 开启

### 2. 创建对象类型

#### UserProfile（用户表）
| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| userId | String | ✅ | 用户ID |
| username | String | | 用户昵称 |
| avatar | String | | 头像URL |
| email | String | | 邮箱 |
| createdAt | Long | | 创建时间 |
| updatedAt | Long | | 更新时间 |
| llmQuotaDaily | Integer | | LLM每日配额 |
| llmQuotaUsed | Integer | | LLM已用配额 |
| imageQuotaDaily | Integer | | 图片每日配额 |
| imageQuotaUsed | Integer | | 图片已用配额 |
| lastQuotaReset | Long | | 上次配额重置时间 |

#### DailyRecord（每日记录表）
| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| recordId | String | ✅ | 记录ID |
| userId | String | | 用户ID |
| date | String | | 日期（YYYY-MM-DD） |
| mood | String | | 心情 |
| events | String | | 事件列表（JSON） |
| summary | String | | 每日总结 |
| comicScript | String | | 漫画脚本（JSON） |
| comicImages | String | | 漫画图片URL（JSON） |
| createdAt | Long | | 创建时间 |
| updatedAt | Long | | 更新时间 |

#### ComicImage（漫画图片表）
| 字段名 | 类型 | 主键 | 说明 |
|--------|------|------|------|
| imageId | String | ✅ | 图片ID |
| userId | String | | 用户ID |
| recordId | String | | 记录ID |
| panelIndex | Integer | | 分镜索引 |
| prompt | String | | 生成提示词 |
| imageUrl | String | | 图片URL |
| status | String | | 状态 |
| taskId | String | | 任务ID |
| createdAt | Long | | 创建时间 |

### 3. 配置权限

在「云数据库」→「权限管理」设置：
- 读权限：所有用户
- 写权限：仅数据拥有者
- 删除权限：仅数据拥有者

### 4. 导出对象类型

1. 点击「导出」
2. 选择格式：`JSON`
3. 下载 `objecttypes.json`
4. 客户端会自动使用此配置

---

## 🖼️ Cloud Storage配置

### 1. 开通云存储

1. 进入「构建」→「云存储」
2. 点击「立即开通」
3. 选择存储位置：`中国-北京`

### 2. 创建存储桶

1. 存储桶名称：`momo-images`
2. 访问权限：`私有读写`
3. 配额：`5GB`（免费额度）

### 3. 配置CORS规则

添加CORS规则：
```json
{
  "AllowedOrigin": ["*"],
  "AllowedMethod": ["GET", "PUT", "POST", "DELETE"],
  "AllowedHeader": ["*"],
  "MaxAgeSeconds": 3600
}
```

### 4. 创建目录结构

```
momo-images/
  └── comics/
      └── {userId}/
          └── {recordId}/
              ├── panel_0.png
              ├── panel_1.png
              ├── panel_2.png
              ├── panel_3.png
              └── panel_4.png
```

---

## ⚡ Cloud Functions配置

### 1. 开通云函数

1. 进入「构建」→「云函数」
2. 点击「立即开通」
3. 选择区域：`中国-北京`

### 2. 创建函数

#### 函数1：llm-daily-summary
```
- 函数名称：llm-daily-summary
- 运行时：Node.js 18
- 内存：512MB
- 超时：60秒
- 代码：上传 cloud-functions/llm-daily-summary.js
```

#### 函数2：llm-comic-script
```
- 函数名称：llm-comic-script
- 运行时：Node.js 18
- 内存：512MB
- 超时：60秒
- 代码：上传 cloud-functions/llm-comic-script.js
```

#### 函数3：image-generate
```
- 函数名称：image-generate
- 运行时：Node.js 18
- 内存：1024MB
- 超时：120秒
- 代码：上传 cloud-functions/image-generate.js
```

### 3. 配置环境变量

在每个云函数的「配置」→「环境变量」添加：

```
DEEPSEEK_API_KEY=sk-xxxxxxxxxxxxxxxx
JIMENG_ACCESS_KEY=AKxxxxxxxxxxxxxxxx
JIMENG_SECRET_KEY=xxxxxxxxxxxxxxxx
```

### 4. 测试云函数

在「测试」页面运行测试：

**llm-daily-summary测试输入：**
```json
{
  "userId": "test_user_001",
  "events": [
    "09:00 起床，阳光明媚",
    "10:30 写代码，完成了新功能",
    "12:00 午餐，吃了美味的面条",
    "15:00 运动，跑步5公里",
    "20:00 看书，读完一章"
  ],
  "mood": "开心"
}
```

---

## 🔑 API密钥配置

### 1. DeepSeek API密钥

1. 访问 [DeepSeek开放平台](https://platform.deepseek.com/)
2. 注册并登录
3. 进入「API Keys」
4. 创建新密钥
5. 复制密钥：`sk-xxxxxxxxxxxxxxxx`

**价格：**
- 输入：¥0.001/1K tokens
- 输出：¥0.002/1K tokens
- 每日总结约消耗：200 tokens（¥0.0004）

### 2. 即梦AI密钥（火山引擎）

1. 访问 [火山引擎控制台](https://console.volcengine.com/)
2. 注册并完成实名认证
3. 进入「视觉智能」→「即梦AI」
4. 创建密钥：
   - Access Key ID：`AKxxxxxxxxxxxxxxxx`
   - Secret Access Key：`xxxxxxxxxxxxxxxx`

**价格：**
- 免费试用：500秒时长
- 付费：¥0.05-0.1/张
- 每日漫画（5张）：约¥0.25-0.5

---

## 📱 客户端配置

### 1. 配置oh-package.json5

添加华为云服务依赖：
```json
{
  "dependencies": {
    "@hms.core.cloudDatabase": "^1.0.0",
    "@hms.core.cloudStorage": "^1.0.0",
    "@hms.core.cloudFunction": "^1.0.0",
    "@kit.AccountKit": "^1.0.0"
  }
}
```

### 2. 同步依赖

在DevEco Studio中：
1. 点击顶部菜单「File」→「Sync and Refresh Project」
2. 或者运行：`ohpm install`

### 3. 配置AGConnect

在 `EntryAbility.ets` 中初始化：
```typescript
import agconnect from '@hms.core.agconnect';

onCreate(want, launchParam) {
  // 初始化AGConnect
  agconnect.AGConnectInstance.initialize(this.context);
  
  // 其他初始化代码...
}
```

---

## ✅ 验证配置

### 1. 测试Account Kit

运行应用，检查：
- ✅ 华为账号登录弹窗正常显示
- ✅ 登录成功后能获取用户信息
- ✅ 日志显示：`用户认证完成: HuaweiAccountKit`

### 2. 测试Cloud DB

```typescript
const cloudDB = CloudDBService.get();
await cloudDB.initialize();
const user = await cloudDB.getUser('test_user_id');
console.log('用户信息:', user);
```

### 3. 测试Cloud Storage

```typescript
const storage = CloudStorageService.get();
await storage.initialize();
const url = await storage.uploadImage('/path/to/test.png', 'test/test.png');
console.log('上传成功:', url);
```

### 4. 测试Cloud Functions

```typescript
const cloudFunc = CloudFunctionService.get();
await cloudFunc.initialize();
const result = await cloudFunc.generateDailySummary({
  userId: 'test',
  events: ['测试事件'],
  mood: '开心'
});
console.log('总结:', result.content);
```

---

## 🐛 常见问题

### 1. Account Kit登录失败

**错误：** `Account Kit initialization failed`

**解决：**
- 检查 `agconnect-services.json` 是否正确放置
- 检查包名是否匹配：`com.agent.momo`
- 检查签名证书SHA-256是否正确配置

### 2. Cloud DB连接失败

**错误：** `CloudDB initialization failed`

**解决：**
- 检查存储区名称是否为 `MomoDataZone`
- 检查对象类型是否已创建
- 检查网络权限是否添加

### 3. 云函数调用失败

**错误：** `Function call failed: timeout`

**解决：**
- 检查环境变量是否正确配置
- 检查API密钥是否有效
- 增加函数超时时间

### 4. 图片上传失败

**错误：** `Upload failed: permission denied`

**解决：**
- 检查存储桶权限设置
- 检查CORS规则是否配置
- 检查文件路径是否正确

---

## 📊 配额说明

### 华为云服务免费额度

| 服务 | 免费额度 | 超出后价格 |
|------|----------|-----------|
| Cloud DB | 2GB存储 + 50万次读写/月 | ¥0.1/GB/月 |
| Cloud Storage | 5GB存储 + 50GB流量/月 | ¥0.1/GB/月 |
| Cloud Functions | 40万GBs/月 | ¥0.0000167/GBs |
| Account Kit | 无限制 | 免费 |

### AI服务成本

| 服务 | 成本 | 说明 |
|------|------|------|
| DeepSeek（每日总结） | ¥0.0004/次 | 200 tokens |
| DeepSeek（漫画脚本） | ¥0.002/次 | 1000 tokens |
| 即梦AI（单张图） | ¥0.05-0.1/张 | 1024x1024 |

**单用户日均成本：** ¥0.5/天（1次总结 + 1次脚本 + 5张图）

**100用户月成本：** ¥1500（华为云免费 + AI服务按量付费）

---

## 🚀 下一步

配置完成后，继续：
1. [快速开始指南](快速开始.md)
2. [客户端测试指南](客户端测试指南.md)
3. [API文档](AI服务架构文档.md)

---

**配置帮助文档生成时间：** 2026-09-18
