# 华为账号登录 - 快速开始

## 🚀 5分钟快速集成

### 第一步：确认文件已创建

以下文件已经创建完成：

```
✅ entry/src/main/ets/service/auth/HuaweiAccountProvider.ets    // 华为账号Provider
✅ entry/src/main/ets/pages/LoginPage.ets                       // 登录页面
✅ entry/src/main/module.json5                                   // 权限已配置
✅ entry/src/main/resources/base/element/string.json            // 权限说明已添加
```

### 第二步：选择集成方式

#### 方式A：使用登录页面（推荐）

修改 `EntryAbility.ets` 的 `onWindowStageCreate` 方法：

```typescript
onWindowStageCreate(windowStage: window.WindowStage): void {
  // 加载登录页面
  windowStage.loadContent('pages/LoginPage', (err) => {
    if (err.code) {
      hilog.error(DOMAIN, 'MoMo', 'Failed to load the content. Cause: %{public}s', JSON.stringify(err));
      return;
    }
    hilog.info(DOMAIN, 'MoMo', 'Succeeded in loading the content.');
    this.setImmersiveWindow();
  });
}
```

#### 方式B：在现有页面中添加登录功能

在任意页面中使用：

```typescript
import { AuthService } from '../service/auth';

@Component
export struct LoginButton {
  private authService: AuthService = AuthService.get();
  
  async handleLogin() {
    this.authService.useHuaweiAccount(getContext(this));
    const user = await this.authService.login();
    console.log('登录成功:', user.nickname);
  }
  
  build() {
    Button('华为账号登录')
      .onClick(() => this.handleLogin())
  }
}
```

#### 方式C：应用启动时自动登录

参考 `EntryAbility_WithHuaweiLogin.ets` 示例文件，在 `onCreate` 中初始化：

```typescript
onCreate(want: Want, launchParam: AbilityConstant.LaunchParam): void {
  // ... 现有代码 ...
  
  // 初始化华为账号登录
  const authService = AuthService.get();
  authService.useHuaweiAccount(this.context);
  authService.initialize(); // 异步，不阻塞启动
}
```

### 第三步：测试登录功能

1. **编译运行应用**
   ```bash
   # 如果使用DevEco Studio，直接点击运行按钮
   # 或使用命令行
   hvigorw assembleHap
   ```

2. **测试登录流程**
   - 点击"华为账号登录"按钮
   - 首次登录会拉起华为账号授权页面
   - 授权成功后自动返回应用
   - 查看用户信息显示

3. **测试静默登录**
   - 关闭应用
   - 重新打开应用
   - 应该自动登录，无需再次授权

4. **测试登出功能**
   - 点击"登出"按钮
   - 用户信息清除
   - 再次登录需要重新授权

### 第四步：查看日志

使用 hdc 查看登录相关日志：

```bash
# 查看所有认证相关日志
hdc shell hilog | grep "HuaweiAccountProvider\|AuthService\|LoginPage"

# 查看具体错误
hdc shell hilog | grep "ERROR"
```

## 📝 常用代码片段

### 检查登录状态

```typescript
const authService = AuthService.get();
const isLoggedIn = await authService.isLoggedIn();

if (isLoggedIn) {
  const user = await authService.getCurrentUser();
  console.log('当前用户:', user?.nickname);
} else {
  console.log('用户未登录');
}
```

### 获取访问Token

```typescript
const authService = AuthService.get();
const token = await authService.getAccessToken();

// 在API请求中使用
fetch('https://your-api.com/endpoint', {
  headers: {
    'Authorization': `Bearer ${token}`
  }
});
```

### 切换登录方式

```typescript
const authService = AuthService.get();

// 使用华为账号登录
authService.useHuaweiAccount(context);
await authService.login();

// 使用设备ID登录（匿名）
authService.switchProvider(new DeviceAuthProvider());
await authService.login();

// 查看当前登录方式
console.log(authService.getProviderName());
```

## 🎯 完整使用示例

创建一个带登录状态的页面：

```typescript
import { AuthService } from '../service/auth';
import { User } from '../service/auth';

@Entry
@Component
struct MyPage {
  @State isLoggedIn: boolean = false;
  @State userName: string = '';
  
  private authService: AuthService = AuthService.get();
  
  async aboutToAppear() {
    // 检查登录状态
    this.isLoggedIn = await this.authService.isLoggedIn();
    
    if (this.isLoggedIn) {
      const user = await this.authService.getCurrentUser();
      this.userName = user?.nickname || '';
    }
  }
  
  async handleLogin() {
    try {
      this.authService.useHuaweiAccount(getContext(this));
      const user = await this.authService.login();
      
      this.isLoggedIn = true;
      this.userName = user.nickname || '';
    } catch (error) {
      console.error('登录失败:', error);
    }
  }
  
  async handleLogout() {
    try {
      await this.authService.logout();
      this.isLoggedIn = false;
      this.userName = '';
    } catch (error) {
      console.error('登出失败:', error);
    }
  }
  
  build() {
    Column() {
      if (this.isLoggedIn) {
        Text(`欢迎，${this.userName}`)
        Button('登出').onClick(() => this.handleLogout())
      } else {
        Button('华为账号登录').onClick(() => this.handleLogin())
      }
    }
  }
}
```

## ⚠️ 注意事项

1. **Context必须设置**
   - 调用 `login()` 前必须先调用 `useHuaweiAccount(context)`
   - Context通常用 `getContext(this)` 获取

2. **权限配置**
   - 已自动配置 `INTERNET` 和 `GET_NETWORK_INFO` 权限
   - 首次运行时系统会请求用户授权

3. **错误处理**
   - 所有异步方法都可能抛出异常
   - 建议使用 try-catch 包裹登录代码

4. **Token管理**
   - 当前使用Mock Token，生产环境需要对接后端
   - 授权码在 `LoginResponse.token.accessToken` 中
   - 需要将授权码发送到后端换取真实Token

## 📚 下一步

- 📖 阅读[完整文档](./华为账号登录集成文档.md)
- 🔧 配置后端Token服务
- 🎨 自定义登录页面UI
- 🔐 实现Token刷新机制

## 🆘 遇到问题？

### 编译错误：找不到 authentication 模块

**解决方案**：确保使用 HarmonyOS NEXT SDK（API 12+）

### 登录时提示"Context未设置"

**解决方案**：在调用 `login()` 前先调用：
```typescript
authService.useHuaweiAccount(getContext(this));
```

### 静默登录总是失败

**解决方案**：这是正常的，首次登录和授权过期时都会失败。系统会自动降级到授权登录。

### 授权后无响应

**解决方案**：检查日志，确认是否有错误信息：
```bash
hdc shell hilog | grep "ERROR"
```

## ✅ 验收清单

完成集成后，请确认以下功能：

- [ ] 点击登录按钮能拉起华为账号授权页面
- [ ] 授权成功后能正常返回应用
- [ ] 能正确显示用户昵称和头像
- [ ] 登出后用户信息被清除
- [ ] 重新打开应用能自动静默登录
- [ ] 日志中没有错误信息

全部通过？恭喜你完成集成！🎉
