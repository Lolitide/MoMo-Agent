# 默默 Mo Mo - 云服务架构设计方案

## 📐 架构重构说明

### 当前问题
- ❌ 用户需要自己配置API Key（不友好）
- ❌ LLM和文生图混在一起（职责不清）
- ❌ 没有用户系统和鉴权
- ❌ 打包时暴露后端配置（不安全）

### 目标架构
- ✅ 云服务模式：后端统一管理API Key
- ✅ 用户鉴权：JWT Token认证
- ✅ 服务分离：LLM服务 + 文生图服务
- ✅ 打包分离：客户端只包含云服务API地址

---

## 🏗️ 新架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                      HarmonyOS 客户端                        │
├─────────────────────────────────────────────────────────────┤
│  AIService (统一入口)                                        │
│    ├─ CloudBackendService (云服务接口)                       │
│    │   ├─ 用户登录/注册                                      │
│    │   ├─ Token刷新                                          │
│    │   └─ 请求拦截器(自动添加Token)                          │
│    ├─ LLMService (对话服务)                                  │
│    │   ├─ 每日总结生成                                       │
│    │   ├─ 记忆纠正                                           │
│    │   └─ 通用对话                                           │
│    ├─ ImageGenService (文生图服务)                           │
│    │   ├─ 漫画分镜生成                                       │
│    │   ├─ 场景图生成                                         │
│    │   └─ 风格定制                                           │
│    └─ MockService (离线演示，无需Token)                      │
└─────────────────────────────────────────────────────────────┘
                              ↕ HTTPS
┌─────────────────────────────────────────────────────────────┐
│                    默默云服务后端（待开发）                    │
├─────────────────────────────────────────────────────────────┤
│  API Gateway (统一入口 + JWT验证)                            │
│    ↓                                                         │
│  ├─ /api/auth/register                                       │
│  ├─ /api/auth/login                                          │
│  ├─ /api/auth/refresh                                        │
│  │                                                            │
│  ├─ /api/llm/daily-summary      (需要Token)                  │
│  ├─ /api/llm/comic-script       (需要Token)                  │
│  ├─ /api/llm/memory-correction  (需要Token)                  │
│  ├─ /api/llm/chat               (需要Token)                  │
│  │                                                            │
│  ├─ /api/image/generate         (需要Token)                  │
│  ├─ /api/image/comic-panel      (需要Token)                  │
│  └─ /api/image/status/{taskId}  (需要Token，查询生成进度)     │
│                                                               │
│  Business Logic (业务逻辑层)                                  │
│    ├─ UserService (用户管理)                                 │
│    ├─ TokenService (Token生成/验证)                          │
│    ├─ QuotaService (用量配额管理)                            │
│    └─ AuditService (审计日志)                                │
│                                                               │
│  Third-party Integration (第三方服务集成)                     │
│    ├─ DeepSeek API (LLM服务)                                 │
│    ├─ Stable Diffusion API (文生图)                          │
│    └─ 阿里云百炼/腾讯云 (备选)                                │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔑 鉴权方案：JWT Token

### Token流程
```
用户打开App (首次)
    ↓
匿名注册 / 设备ID注册
    ↓
后端返回: { accessToken, refreshToken, userId }
    ↓
客户端存储到Preferences
    ↓
后续请求自动携带: Authorization: Bearer {accessToken}
    ↓
Token过期 (30分钟)
    ↓
自动用refreshToken刷新
    ↓
后端返回新accessToken
```

### Token结构
```json
{
  "accessToken": "eyJhbGciOiJIUzI1NiIs...",  // 短期token，30分钟
  "refreshToken": "eyJhbGciOiJIUzI1NiIs...", // 长期token，30天
  "userId": "uuid-xxx-xxx",
  "expiresIn": 1800  // 秒
}
```

---

## 🎨 文生图服务方案

### 方案对比

| 方案 | 优点 | 缺点 | 成本 | 推荐度 |
|------|------|------|------|--------|
| **阿里云百炼 SD** | 国内快速、稳定、中文支持好 | 稍贵 | ¥0.08/次 | ⭐⭐⭐⭐⭐ |
| **ModelsLab API** | 便宜、模型多 | 国外访问慢 | ¥0.03/次 | ⭐⭐⭐ |
| **自建SD服务** | 完全控制 | 运维成本高 | GPU服务器费用 | ⭐⭐ |

### 推荐方案：**阿里云百炼 + Stable Diffusion 3.5**

**理由**：
1. 国内访问快速稳定
2. 中文prompt适配好（重要！）
3. API简单，官方SDK支持
4. 成本可控（¥0.08/次）

**API示例**（参考：[阿里云文生图API](http://help.aliyun.com/zh/model-studio/developer-reference/stable-diffusion-quick-start)）：
```typescript
POST https://dashscope.aliyuncs.com/api/v1/services/aigc/text2image/image-synthesis
Authorization: Bearer {api-key}
Content-Type: application/json

{
  "model": "stable-diffusion-3.5-large",
  "input": {
    "prompt": "清晨，阳光透过窗帘，一只可爱的AI桌宠坐在窗台上，卡通风格，温暖色调"
  },
  "parameters": {
    "size": "1024*1024",
    "n": 1
  }
}
```

---

## 🔧 客户端实现要点

### 1. 配置管理（分离云服务地址和离线模式）

```typescript
// service/ai/CloudConfig.ets
export interface CloudConfig {
  baseUrl: string;        // 云服务地址
  enableOfflineMode: boolean;  // 是否启用离线模式
}

// 打包配置（resources/rawfile/cloud_config.json）
{
  "baseUrl": "https://api.momo.example.com",
  "enableOfflineMode": true  // 开发时true，生产时false
}
```

### 2. Token管理

```typescript
// service/auth/TokenManager.ets
class TokenManager {
  private accessToken: string;
  private refreshToken: string;
  
  async getValidToken(): Promise<string> {
    if (this.isTokenExpired(this.accessToken)) {
      await this.refreshAccessToken();
    }
    return this.accessToken;
  }
  
  async refreshAccessToken(): Promise<void> {
    // 调用 /api/auth/refresh
  }
}
```

### 3. 请求拦截器（自动添加Token）

```typescript
// service/CloudHttpClient.ets
class CloudHttpClient extends HttpClient {
  async request(options: HttpRequestOptions) {
    // 自动添加Authorization头
    const token = await TokenManager.get().getValidToken();
    options.headers = {
      ...options.headers,
      'Authorization': `Bearer ${token}`
    };
    return super.request(options);
  }
}
```

---

## 🚀 开发阶段计划

### 阶段1：客户端重构（本周）
- [ ] 重构AIService为云服务模式
- [ ] 实现TokenManager
- [ ] 实现CloudBackendService
- [ ] 分离LLMService和ImageGenService
- [ ] Mock模式完善（无需Token）

### 阶段2：后端MVP（下周或外包）
- [ ] 搭建基础框架（建议Node.js + NestJS）
- [ ] 实现用户系统（匿名注册）
- [ ] 实现JWT鉴权
- [ ] 接入DeepSeek API
- [ ] 接入阿里云百炼文生图
- [ ] 部署到云服务器

### 阶段3：集成联调
- [ ] 客户端接入真实后端
- [ ] E2E测试
- [ ] 性能优化
- [ ] 配额和计费逻辑

---

## 💰 成本估算

### LLM成本（DeepSeek）
- 每日总结：~¥0.001/次
- 漫画脚本：~¥0.002/次
- 记忆纠正：~¥0.0008/次

### 文生图成本（阿里云百炼）
- 单张图：~¥0.08/次
- 每日漫画（5格）：~¥0.4/天

### 服务器成本
- 轻量应用服务器：~¥30-50/月
- 对象存储（图片）：~¥10/月

### 总成本估算
- **用户日均成本**：¥0.5/天（LLM + 5格漫画）
- **月成本**：¥15/用户
- **100用户/月**：¥1500（可行）
- **1000用户/月**：¥15000（需考虑配额限制或付费订阅）

---

## 📋 下一步具体行动

### 优先级P0（今天完成）
1. ✅ 重构客户端架构文档（当前文档）
2. ⏳ 实现TokenManager
3. ⏳ 实现CloudBackendService基础框架
4. ⏳ 分离LLMService和ImageGenService
5. ⏳ 更新Mock模式（模拟云服务响应）

### 优先级P1（明天）
6. 设计后端API接口规范
7. 选择后端技术栈（推荐NestJS）
8. 搭建后端MVP框架

### 优先级P2（本周内）
9. 实现后端核心功能
10. 部署到测试环境
11. 客户端联调

---

## 🤔 需要确认的问题

1. **后端开发方式**：
   - 选项A：你自己开发（需要后端技术栈）
   - 选项B：外包/合作开发
   - 选项C：我帮你生成后端完整代码（NestJS）

2. **用户系统方案**：
   - 选项A：完全匿名（设备ID注册）
   - 选项B：手机号注册（需要短信服务）
   - 选项C：三方登录（微信/华为账号）

3. **文生图服务商**：
   - 选项A：阿里云百炼（推荐，稳定快速）
   - 选项B：ModelsLab（便宜，但可能慢）
   - 选项C：先用Mock，后续再接

4. **部署方式**：
   - 选项A：阿里云/腾讯云（推荐）
   - 选项B：华为云（鸿蒙生态）
   - 选项C：Vercel/Railway（简单但有限制）

---

## 📚 参考资料

- [JWT认证最佳实践](https://www.cnblogs.com/michaelshen/p/19824136)
- [阿里云百炼文生图API](http://help.aliyun.com/zh/model-studio/developer-reference/stable-diffusion-quick-start)
- [HarmonyOS云服务接入](https://www.cnblogs.com/MyYTT/p/18887794)
- [移动应用后端架构设计](https://www.volcengine.com/article/35534)
