# 客户端测试指南

## 📱 当前功能状态

### ✅ 已实现并可测试
1. **设备ID自动登录**
2. **Token自动管理**
3. **Mock模式AI服务**（每日总结、漫画脚本、记忆纠正、图像生成）
4. **架构分层**（Auth → Backend → AI）

### ❌ 未实现（需要开发）
1. **华为账号登录**（AccountKitProvider）
2. **真实云服务调用**（需要后端API）
3. **真实文生图生成**（需要接入即梦API）

---

## 🧪 测试方法

### 方法1：使用新的测试页面（推荐）

#### 步骤1：将测试页面加入路由
编辑 `entry/src/main/resources/base/profile/main_pages.json`：
```json
{
  "src": [
    "pages/MainPage",
    "pages/A_Home",
    ...其他页面...,
    "pages/B_AITest_V2"  // 添加这一行
  ]
}
```

#### 步骤2：从MainPage跳转到测试页
有两个方法：

**方法A：修改启动页（临时测试）**
编辑 `entry/src/main/ets/entryability/EntryAbility.ets`：
```typescript
onWindowStageCreate(windowStage: window.WindowStage) {
  windowStage.loadContent('pages/B_AITest_V2', (err, data) => {
    // 直接加载测试页
  });
}
```

**方法B：在现有页面添加跳转按钮**
在 `A_Settings.ets` 设置页添加：
```typescript
Button('AI服务测试')
  .onClick(() => {
    const navPathStack = AppRouter.getStack();
    navPathStack?.pushPath({ name: 'B_AITest_V2' });
  })
```

#### 步骤3：运行并测试
1. DevEco Studio编译运行
2. 看到测试页面
3. 点击各个测试按钮
4. 查看结果和Hilog日志

---

### 方法2：在Ability中直接测试（无需UI）

编辑 `entry/src/main/ets/entryability/EntryAbility.ets`：
```typescript
import { AIService } from '../service/ai/AIService';
import { AuthService } from '../service/auth/AuthService';
import { Logger } from '../utils/Logger';
import hilog from '@ohos.hilog';

async onWindowStageCreate(windowStage: window.WindowStage) {
  // 测试代码
  await this.testServices();
  
  // 原有代码
  windowStage.loadContent('pages/MainPage', ...);
}

async testServices() {
  try {
    hilog.info(0x0000, 'EntryAbility', '===== 开始测试服务 =====');
    
    // 1. 测试认证
    const authService = AuthService.get();
    await authService.initialize();
    const user = await authService.getCurrentUser();
    hilog.info(0x0000, 'EntryAbility', `用户ID: ${user?.userId}`);
    
    const token = await authService.getAccessToken();
    hilog.info(0x0000, 'EntryAbility', `Token: ${token.substring(0, 30)}...`);
    
    // 2. 测试AI服务
    const aiService = AIService.get();
    await aiService.initialize();
    hilog.info(0x0000, 'EntryAbility', `AI模式: ${aiService.getProviderName()}`);
    
    // 3. 测试每日总结
    const result = await aiService.generateDailySummary([]);
    hilog.info(0x0000, 'EntryAbility', `生成结果: ${result.substring(0, 100)}...`);
    
    hilog.info(0x0000, 'EntryAbility', '===== 测试完成 =====');
  } catch (error) {
    hilog.error(0x0000, 'EntryAbility', `测试失败: ${error}`);
  }
}
```

---

## 📊 预期测试结果

### 认证服务测试
```
✅ 状态: 已登录 (设备ID登录)
✅ 用户ID: u_abc12345_1726675200000
✅ Token: at_u_abc123_1726675200000_xy8z...
```

### AI服务测试
```
✅ 状态: 已初始化
✅ 运行模式: Mock (离线模式)
```

### 每日总结测试
```json
{
  "summary": "今天是充实的一天。上午专注完成了设计方案...",
  "emotion": "平静而满足",
  "theme": "工作与休息的平衡",
  "insights": [...]
}
```

---

## 🐛 常见问题

### Q1: 编译报错找不到模块
**原因**：新文件未被识别  
**解决**：Clean Project → Rebuild Project

### Q2: 运行时报错"Token不存在"
**原因**：首次运行，Token未初始化  
**解决**：正常现象，AuthService会自动登录并生成Token

### Q3: Mock数据不符合预期
**原因**：MockProvider的预设响应  
**解决**：修改 `service/ai/MockProvider.ets` 中的Mock数据

---

## ⏭️ 下一步开发

1. **华为账号登录**：开发 `AccountKitProvider.ets`
2. **后端API**：开启新对话生成后端代码
3. **真实文生图**：接入即梦API

---

需要我帮你做什么？
- 生成AccountKitProvider？
- 开始后端开发？
- 修改测试代码？
