# 🚀 快速开始 - AI服务验证指南

## 方式1：使用Mock模式（推荐，无需配置）

### 步骤1：编译运行
```bash
# 在DevEco Studio中打开项目
# 点击 Run 'entry' 或按 Shift+F10
```

### 步骤2：导航到测试页面
当前测试页面 `B_AITest.ets` 已创建，但还未加入路由。

**临时方案A**：直接在MainPage中测试
```typescript
// 在 entry/src/main/ets/entryability/EntryAbility.ets 的 onWindowStageCreate 中添加：
import { AIService } from '../service/ai';

async onWindowStageCreate(windowStage: window.WindowStage) {
  // 测试AI服务
  const aiService = AIService.get();
  await aiService.initialize();
  console.log('✅ AI服务初始化成功:', aiService.getProviderName());
  
  // 原有代码...
  windowStage.loadContent('pages/MainPage', ...);
}
```

**临时方案B**：在首页添加测试按钮
```typescript
// 在 A_Home.ets 中添加测试按钮
Button('测试AI')
  .onClick(async () => {
    const aiService = AIService.get();
    await aiService.initialize();
    const result = await aiService.generateDailySummary([]);
    console.log('AI响应:', result);
    promptAction.showToast({ message: 'AI测试完成，查看日志' });
  })
```

### 步骤3：查看日志输出
```bash
# 在DevEco Studio的 Hilog 面板查看：
# [AIService] AI服务初始化完成，当前提供商: Mock (离线模式)
# [MockProvider] Mock响应请求，消息数: 2
# [MockProvider] 返回Mock响应: xxx字符
```

---

## 方式2：使用真实DeepSeek API

### 步骤1：获取API Key
1. 访问 https://platform.deepseek.com
2. 注册并登录
3. 进入 "API Keys" 页面
4. 点击 "Create API Key"
5. 复制生成的密钥（格式：sk-xxxxxxxxxxxxxxxx）

### 步骤2：配置API Key
编辑 `entry/src/main/resources/rawfile/ai_config.json`：
```json
{
  "provider": "deepseek",
  "apiKey": "sk-你的实际密钥",
  "baseUrl": "https://api.deepseek.com/v1",
  "model": "deepseek-chat",
  "timeout": 30000,
  "retry": 3
}
```

### 步骤3：确认网络权限
检查 `entry/src/main/module.json5` 是否包含：
```json
"requestPermissions": [
  {
    "name": "ohos.permission.INTERNET",
    "reason": "$string:internet_permission_reason",
    "usedScene": {
      "abilities": ["EntryAbility"],
      "when": "inuse"
    }
  }
]
```

### 步骤4：运行并测试
与方式1相同，但会看到真实API调用：
```bash
# Hilog输出示例：
# [AIService] 配置加载成功: provider=deepseek
# [DeepSeekProvider] 发送请求到 https://api.deepseek.com/v1
# [HttpClient] POST https://api.deepseek.com/v1/chat/completions
# [HttpClient] 响应码: 200, 数据长度: 458
# [DeepSeekProvider] 生成成功: 156字符, Token用量: 287 (输入187 + 输出100)
# [AIService] AI服务初始化完成，当前提供商: DeepSeek
```

---

## 方式3：集成到现有页面

### 在"今日状态"页面添加AI总结按钮

编辑 `entry/src/main/ets/pages/B_TodayMemory.ets`：

```typescript
import { AIService } from '../service/ai';
import promptAction from '@ohos.promptAction';

// 在页面类中添加状态
@State aiSummary: string = '';
@State isGenerating: boolean = false;

// 添加生成方法
async generateAISummary() {
  this.isGenerating = true;
  try {
    const aiService = AIService.get();
    const events = EventRepository.get().getTodayEvents();
    const resultJson = await aiService.generateDailySummary(events);
    const result = JSON.parse(resultJson);
    this.aiSummary = result.summary;
    promptAction.showToast({ message: '✅ AI总结生成成功' });
  } catch (error) {
    promptAction.showToast({ message: `❌ 生成失败: ${error}` });
  } finally {
    this.isGenerating = false;
  }
}

// 在UI中添加按钮（在适当位置）
Button(this.isGenerating ? '生成中...' : '生成AI总结')
  .enabled(!this.isGenerating)
  .onClick(() => this.generateAISummary())

// 显示结果
if (this.aiSummary) {
  Text(this.aiSummary)
    .fontSize(14)
    .lineHeight(22)
    .padding(16)
}
```

---

## 验证清单

### ✅ 基础验证
- [ ] 应用可以正常编译
- [ ] 应用可以正常启动
- [ ] 日志显示 "AI服务初始化完成"
- [ ] 日志显示当前提供商（Mock或DeepSeek）

### ✅ Mock模式验证
- [ ] Mock模式下可以生成每日总结
- [ ] Mock模式下可以生成漫画脚本
- [ ] Mock模式下可以处理记忆纠正
- [ ] 响应内容符合预期（JSON格式）

### ✅ DeepSeek模式验证（可选）
- [ ] 配置API Key后自动切换到DeepSeek
- [ ] 可以成功调用DeepSeek API
- [ ] 日志显示Token使用情况
- [ ] 生成内容质量符合预期

### ✅ 降级机制验证
- [ ] API Key错误时自动降级到Mock
- [ ] 网络不可用时自动降级到Mock
- [ ] API调用失败后重试3次
- [ ] 重试失败后降级到Mock

---

## 常见问题

### Q1: 编译报错 "找不到@ohos.net.http"
**A**: 检查 SDK 版本，需要 API 9 或更高版本。

### Q2: 运行时报错 "No permission: ohos.permission.INTERNET"
**A**: 确认 `module.json5` 中已添加网络权限，并在设置中授权。

### Q3: DeepSeek API调用超时
**A**: 
1. 检查网络连接
2. 增加 `ai_config.json` 中的 `timeout` 值
3. 降级到Mock模式进行演示

### Q4: Mock响应内容不符合预期
**A**: 查看 `MockProvider.ets`，根据需要修改预设响应内容。

---

## 下一步

D1已完成，建议接下来：

1. **验证基础功能**：确保Mock模式工作正常
2. **配置真实API**：获取DeepSeek API Key并测试
3. **集成到页面**：在今日状态页、今日漫画页集成AI能力
4. **开始D2**：实现Observation Agent，采集用户输入

---

## 技术支持

- 查看 `AI服务架构文档.md` 了解完整架构
- 查看 `service/ai/README.md` 了解详细使用方法
- 查看 `开发日志.md` 了解开发记录
- 查看日志文件（Hilog）排查问题
